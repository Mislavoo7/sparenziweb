var element = document.querySelector(`[data-sections-id="<%= dom_id(@section) %>"]`);
if (element) {
  element.style.transition = "opacity 0.2s ease-out"; // fast equivale
  element.style.opacity = 0;

  setTimeout(function() {
    element.remove();
  }, 200); // match "fast" timing
}
